const ratWrapper = ({
  config,
}) => {
  const get = async ({
    payload,
    header,
    onSuccess,
    onError,
  }) => {

  };

  const create = async ({
    payload,
    header,
    onSuccess,
    onError,
  }) => {

  };

  const update = async ({
    payload,
    header,
    onSuccess,
    onError,
  }) => {

  };

  const destroy = async ({
    payload,
    header,
    onSuccess,
    onError,
  }) => {

  };

  return {
    get,
    create,
    update,
    destroy,
  };
};

module.exports = ratWrapper;
